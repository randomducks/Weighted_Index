@model WeightedIndex.Models.MeasureModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Measure</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.name)
            @Html.ValidationMessageFor(model => model.name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.performanceImpact)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.performanceImpact)
            @Html.ValidationMessageFor(model => model.performanceImpact)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.quickToImplement)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.quickToImplement)
            @Html.ValidationMessageFor(model => model.quickToImplement)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
